build/
build_lapack/
*.o
*.csv
*.json
polar.csv
spanwise_loading.csv
aircraft.json

# ImGui layout state written by aeolion_viewer at runtime
imgui.ini

# Log file the logger's file sink writes at runtime
Aeolion.log

# Sphinx/Doxygen documentation build output
doc/_build/

# vcpkg
vcpkg_installed/

# ...but keep the project's vcpkg manifest, overlay config and CMake presets (checked in)
!vcpkg.json
!vcpkg-configuration.json
!CMakePresets.json
!ports/**/vcpkg.json

# ...and the JSON handoff documents the contract tests parse
!tests/Data/*.json

# LaTeX build byproducts under papers/
papers/**/*.aux
papers/**/*.log
papers/**/*.out
papers/**/*.blg
papers/**/*.fls
papers/**/*.fdb_latexmk
papers/**/*.synctex.gz
